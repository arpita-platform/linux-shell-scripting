#Bash script for RAM monitoring and threshold-based memory alerts.

#!/bin/bash

Free_Space=$(free -mt | grep "Total" | awk '{print $4}')
TH=500

if [[ $Free_Space -lt $TH ]]
then
    echo "Warning!!! The RAM is running low"
else
    echo "RAM space is sufficient - $Free_Space M"
fi
